# ==============================================================================
# Claims Dashboard - Environment Configuration
# ==============================================================================
# Copy this file to .env and configure for your environment

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------

# Server port (default: 3000)
PORT=3000

# Server host binding (default: 0.0.0.0 for Docker, localhost for dev)
HOST=0.0.0.0

# Environment: development | production | test
NODE_ENV=development

# Log level: debug | info | warn | error
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------

# PostgreSQL connection string
# Format: postgres://user:password@host:port/database
# If not set, uses SQLite (in-memory for dev, file for production)
DATABASE_URL=postgres://claude:claude-flow-test@localhost:5432/claude_flow

# Postgres connection (parsed from DATABASE_URL above)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=claude_flow
POSTGRES_USER=claude
POSTGRES_PASSWORD=claude-flow-test

# SQLite file path (only used if DATABASE_URL not set in production)
# SQLITE_PATH=./data/claims.db

# Database connection settings
DB_CONNECT_RETRIES=30
DB_CONNECT_INTERVAL=2

# Skip migrations on startup (set to "true" to skip)
# SKIP_MIGRATIONS=false

# ------------------------------------------------------------------------------
# Authentication
# ------------------------------------------------------------------------------

# Team authentication secret (used for simple name+secret auth)
# Change this in production!
TEAM_SECRET=changeme-in-production

# Auth header name (default: X-Team-Secret)
# AUTH_HEADER_NAME=X-Team-Secret

# Disable authentication (NOT recommended for production)
# AUTH_DISABLED=false

# ------------------------------------------------------------------------------
# GitHub Integration (Optional)
# ------------------------------------------------------------------------------

# Repository owner (e.g., "ruvnet")
# GITHUB_OWNER=

# Repository name (e.g., "claude-flow")
# GITHUB_REPO=

# GitHub Personal Access Token (for private repos or higher rate limits)
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxx

# Filter issues by labels (comma-separated)
# GITHUB_LABELS=ready,approved

# Polling interval in seconds (default: 60)
# GITHUB_POLL_INTERVAL=60

# Include closed issues in sync (default: false)
# GITHUB_INCLUDE_CLOSED=false

# Maximum issues to fetch per poll (default: 100)
# GITHUB_PER_PAGE=100

# ------------------------------------------------------------------------------
# Claude Flow Integration (Optional)
# ------------------------------------------------------------------------------

# Hook receiver URL for MCP task events
# CLAUDE_FLOW_HOOK_URL=http://localhost:3001/hooks

# ------------------------------------------------------------------------------
# WebSocket Configuration
# ------------------------------------------------------------------------------

# WebSocket path (default: /ws)
# WS_PATH=/ws

# Heartbeat interval in milliseconds (default: 30000)
# WS_HEARTBEAT_INTERVAL=30000

# ------------------------------------------------------------------------------
# CORS Configuration (Production)
# ------------------------------------------------------------------------------

# Allowed origins (comma-separated, or * for all)
# CORS_ORIGINS=https://yourdomain.com

# ------------------------------------------------------------------------------
# Docker-specific
# ------------------------------------------------------------------------------

# When running in Docker, use these for internal networking:
# DATABASE_URL=postgres://claude:claude-flow-test@ruvector-postgres:5432/claude_flow
# CLAUDE_FLOW_HOOK_URL=http://host.docker.internal:3001/hooks

ORCHESTRATOR_WORKING_DIR=/path/to/your/working/dir
ORCHESTRATOR_MAX_AGENTS=1